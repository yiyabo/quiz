# 后端配置
# 开发环境可以用 0.0.0.0，生产环境建议用具体 IP
# 如果遇到 [Errno -2] 错误，请改成服务器的公网 IP，如：119.147.24.196
HOST=127.0.0.1
PORT=6667
RELOAD=true

# CORS配置（生产环境请设置具体域名，多个用逗号分隔）
# 开发环境: ALLOWED_ORIGINS=*
# 生产环境: ALLOWED_ORIGINS=http://119.147.24.196:6666
ALLOWED_ORIGINS=*

# 数据库配置（可选，默认使用SQLite）
# DATABASE_URL=sqlite:///./quiz_platform.db
